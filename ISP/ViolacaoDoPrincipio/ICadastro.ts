interface ICadastro {
    validarDados();
    enviarEmail();
    salvarDados();
}

// interface implementa contratos, contratos são os métodos e propriedades criadas dentro da interface, que
// são de implementação ou uso obrigatorio nas classe que implementarem.