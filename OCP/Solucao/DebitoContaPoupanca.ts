class DebitoContaPoupanca extends DebitoConta {

    debitar(valor: number, conta: string) {
        // validar anivesário da conta
        // lógica para debitar a conta poupança
        return this.numeroTransacao;
    }
}