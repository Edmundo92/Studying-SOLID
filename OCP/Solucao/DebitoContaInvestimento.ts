class DebitoContaInvestimento extends DebitoConta {

    debitar(valor: number, conta: string) {
        // lógica para debitar a conta investimento
        // isentar conta
        return this.numeroTransacao;
    }
}